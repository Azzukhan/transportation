version: "3.9"

services:
  api:
    image: your-registry/transportation-api:latest
    container_name: transportation-api
    env_file:
      - .env
    ports:
      - "8000:8000"
    restart: unless-stopped
